# SeederKit Docs

This directory is the source of truth for the project plan.

Start here when resuming work:

- [Architecture Plan](architecture-plan.md)

The older `notes/` directory contains development notes and reasoning snapshots. Those notes are useful context, but this docs directory should hold the current plan and checkpoint status.
